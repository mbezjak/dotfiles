# -*- mode: snippet -*-
# name: New release steps
# key: newrsteps
# expand-env: ((yas/indent-line 'fixed))
# --
Release steps:

1. Add \`Changelog.md\` entry
2. Update version in \`README.md\`
3. Git work:

        $ git tag --annotate $version
        $ git push
        $ git push --tags
