# -*- mode: snippet -*-
# name: notok
# key: notok
# --
trap onexit EXIT
onexit() {
    local exit_status=\${1:-$?}
    [[ $exit_status -ne 0 ]] && echo "$(tput setaf 1)NOT OK$(tput sgr0)"
    exit $exit_status
}
