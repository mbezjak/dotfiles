# -*- mode: snippet -*-
# name: New Writes typeclass
# key: writes
# --
implicit def writes${2:[T : Writes]}: Writes[${1:Container[T]}] = new Writes[$1] {
  def writes(o: $1) = ${3:JsBoolean(false)}
}