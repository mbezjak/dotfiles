#!/bin/bash
#

in=$(hg --quiet incoming --template '{desc}\n')
out=$(hg --quiet outgoing --template '{desc}\n')

[[ -n "$in"  ]] && echo "$in" | awk '{print "[in ] " $0}'
[[ -n "$out" ]] && echo "$in" | awk '{print "[out] " $0}'

exit 0
