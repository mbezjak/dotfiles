#!/bin/bash
#

git fetch --quiet --all

in=$(git log --oneline master..origin/master)
out=$(git log --oneline origin/master..master)

[[ -n "$in"  ]] && echo "$in" | awk '{print "[in ] " $0}'
[[ -n "$out" ]] && echo "$in" | awk '{print "[out] " $0}'

exit 0
