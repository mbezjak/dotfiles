#!/bin/bash
#
# For each grails project in workspace execute command given as argument $1.
#
# example:
#  $ grailsforeach 'grailshasplugin foo'

set -o errexit

for project in $(projects --grails)
do
    cd "$project"
    eval "$@" # potential security problem
done

exit 0
